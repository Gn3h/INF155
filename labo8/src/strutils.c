#include "strutils.h"

int str_taille(char *chaine) {
    int compteur = 0;

    while(*chaine != '\0') {
        compteur++;
        chaine++;
    }

    return compteur;
}

int tr_copie(char *destination, char *source, int taille_max) {
    
    
    return 0;
}
